import { LoginCard } from "../components/Login/LoginCard"

export const Login = () => {
  return (
    <div id="login-card-container" className="h-full border border-red-400 flex items-center justify-center">
      <LoginCard/>
    </div>
  )
}